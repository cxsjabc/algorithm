#include <stdio.h> 
#include <stdlib.h> 
#include <math.h> 

// not completed
int s[20001];
int b[20001];
int d[20001];

int main()
{
	int i = 0;
	char c;

	while((c = getchar()) != EOF)
		s[i++] = c;
	

	return 0;
}

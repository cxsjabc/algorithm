#include "common_local.h"

typedef struct _dclistnode
{
	int		v;
	struct _dclistnode	*next;
	struct _dclistnode	*prev;
}dclistnode;

void	dclist_insert(dclistnode *node)

int main()
{
    printf("%d\n", 0);
    return 0;
}
